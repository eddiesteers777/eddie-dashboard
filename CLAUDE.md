# Southbound Coaching — Project Guide & Handoff

Southbound Coaching (formerly "EddieOS") is Eddie Steers' coaching business platform: a public marketing site for guests, plus an installable app (PWA) where clients train and Eddie coaches them. It covers online coaching, running and strength programming, and 1-on-1 or group soccer training.

- **Live site:** https://southboundcoaching.com (GitHub Pages custom domain via the `CNAME` file; deploys automatically from `main`). The old https://eddiesteers777.github.io/eddie-dashboard/ address redirects there. Absolute URLs (share tags, the COROS `oauth/client-metadata.json` client ID) use the domain; everything else uses relative paths, so keep it that way.
- **Repo:** `eddiesteers777/eddie-dashboard`
- **Deeper background:** `docs/PRODUCT_ARCHITECTURE.md` (the original audit, account/role design, and roadmap)

---

## Directions for a new chat (read first)

1. **Read this file, then `docs/PRODUCT_ARCHITECTURE.md`,** before changing anything. Inspect the real code instead of trusting summaries. Both docs describe intent, and the code is the truth.
2. **Workflow Eddie expects:**
   - Build on a feature branch, test it, commit, and push the branch.
   - Wait for Eddie to say **"push it"** (or "push"). Only then merge to `main` (`git checkout main && git pull origin main && git merge --no-edit <branch> && git push origin main`) and switch back to the branch.
   - Eddie paces big work with short messages like "step 8". Do one roadmap step fully (build → test → report), then stop.
3. **Eddie isn't a developer.** Any step he has to do himself (Firebase Console, GitHub uploads) needs plain, click-by-click directions. Screenshots from him are common, so read them carefully.
4. **Challenge ideas when there's a better way.** Eddie explicitly asked for pushback, not agreement.
5. **Firestore rules are NOT auto-deployed.** Whenever `firestore.rules` changes, tell Eddie to paste the **whole file** into Firebase Console → Firestore Database → **Rules** → Publish. A new collection without its rules fails with permission-denied.
6. **Never put Eddie's email or other personal info in the code.** He chose to keep it out of the public repo.

---

## The three experiences

| Who | What they see | How it's decided |
|---|---|---|
| **Guest** (signed out) | Public site only: Home, About, Packages, Contact, Get the App, Apply | `js/loadHeader.js` sends signed-out visitors on any app page to `home.html` |
| **Client** (approved, `status: "active"`) | The app, filtered to the services Eddie granted them | `services[]` on their profile, read by `js/navAccess.js` |
| **Coach / admin** (`isCoachApproved: true`) | Everything, plus the Coach section | Eddie's own account. Approved coaches can approve anyone. |

A new sign-in starts as a **pending client** with no services, so it sees almost nothing until approved. That's by design.

---

## Page map

**Public site** (uses `components/publicHeader.html` + `js/loadPublicHeader.js` + `css/public.css`)

| Page | Purpose |
|---|---|
| `home.html` | Guest "dashboard": hero photo, the four offerings as photo cards, meet-your-coach, what the app gives clients, how it works |
| `about.html` | Photo + bio (bio text is still placeholder) + 3-photo gallery |
| `packages.html` | Online Coaching / 1-on-1 Soccer / Group Soccer packages (prices are still `$—`) |
| `coaching.html`, `soccer.html` | Offering detail pages (linked from Home cards and footers, not the top nav) |
| `apply.html` | Intake form (requires Google sign-in). Writes the request onto the applicant's pending profile. |
| `contact.html` | "Ask a question" form, **no sign-in**: name, email and/or phone, topic, who's training (+ athlete age), message. Saves to `inquiries` and emails a coach alert. `?about=soccer_group` etc. preselects the topic. Every page's CTA band has an "Ask a Question" button next to Apply. |
| `install.html` | "Get the App" install instructions (own standalone header) |
| `privacy.html` | Plain-language privacy page (what's collected, Firebase/EmailJS, who sees it, deletion via Contact). Linked from every public footer, the apply sign-in and the contact form. Keep it true when data handling changes. |

Photos live in `images/`. `images/README.md` lists the exact filenames. A missing photo shows a styled placeholder (the `<img onerror="this.remove()">` + `.pub-photo-fallback` pattern).

**App: client pages** (use `components/header.html` + `js/loadHeader.js`)

| Area | Pages |
|---|---|
| Today | `index.html`. **Clients** (`js/todayClient.js`): today's workout(s) as big cards with Mark done + Start/Open, "Rest day · Next: ..." otherwise, a "This Week" strip (one line per day, done/missed marks, totals) linking to My Plan, then the coach card and stats. **Coach**: his own dashboard (marathon plan, planner) in `js/app.js`, unchanged. |
| Train | `running.html`, `strength.html`, `cross-training.html` |
| Health | `nutrition.html`, `fueling.html` |
| Habits | `habits.html` |
| More | `programs.html`, `pace-calculator.html`, `settings.html`, `more.html` |
| Coach-only personal tools | `marathon.html`, `75day.html`, `planner.html` (Eddie's own calendar: add/edit/delete events, saved privately as `planner-events` via cloud sync, `js/plannerEvents.js`; today's events show on Today; no dates live in the code), `analytics.html`, `weekly-review.html`, `gear.html` (Eddie's own; `data-requires="coach"` in nav, More, search and the Today cards, and `COACH_ONLY_PAGES` in `js/loadHeader.js` sends a client who opens one by link back to Today) |
| Client coaching | `schedule.html` (book sessions), `checkin.html` (weekly check-in), `profile.html` ("My Profile": the client intake, prefilled from their application, saved to `clientRecords`), `clients.html?tab=share` ("Connect with Your Coach": get a one-time code for the coach), `updates.html` ("From Your Coach": updates the coach sent, check-in replies and past session notes in one feed; opening it marks updates read), `plan.html` ("My Plan", the client's **Plan** tab: the "Your plan was updated" notice with the coach's note + change list + **Got it**; the coach plan's big picture; and the whole week, one week at a time -- everything from every source with Mark done on each workout -- works offline and for clients training on their own plans too), `workout.html?program=…&date=…` (one workout: the coach's structured steps, why + cue, **Start workout** full-screen step-by-step mode with timers, **Log this run** -- done/skipped, distance, time, live pace, effort 1-10, pain + note, note -- then planned vs actual and the coach's reply; a **Fuel** card (Phase G) on runs that need it -- Before / During (targets + each gel by time and mile from their Fueling Library, caffeinated last) / After, the coach's fueling note first, "Fine-tune in Fueling →" (`fueling.html?type=&miles=&duration=` prefills the run and their last saved details) -- and workout mode shows "Next gel at 0:57" / "Gel now" (with a buzz); `&log=1` opens the log form; the week's run titles and Start/Log it buttons link here; `&kind=strength` is the coach's strength session on that day, `js/strengthSession.js`: exercises numbered 1/2/3A/3B with sets × reps @ weight, RPE/in reserve, rest, coach note and a Demo link; **Start session** goes set by set with weight/reps to adjust and a rest timer, supersets alternating; **Log it** ticks every set with weight × reps, effort, pain, note; then planned vs actual and the coach's reply), all reached via Tools / More. Today's coach card shows "Your plan is ready / was updated" until Got it. Today's coach card prompts "Tell your coach about you" until the profile has a goal and a sport. |

**App: coach pages** (the **Coach** bottom tab on mobile / **Coach** dropdown on desktop)

| Page | Purpose |
|---|---|
| `coach.html` | Dashboard: **Who needs you today** at the top (Phase F: every client's attention items as one list, most urgent first -- pain, change requests, check-ins with their flags, missed/skipped workouts, unopened plan updates, race soon, plan ending, gone quiet, no check-in... -- each linking to the right hub tab; `attentionQueue` in `js/feedbackModel.js`), then new website questions (with Email/Text/Call and Mark answered), pending accounts, booking requests, check-ins to review, active clients. `coach.html#inquiries` jumps to the questions. |
| `clients.html` | Coach: *Coach a Client* (enter a client's code, then the client list: search, filter chips All / Needs attention / Running / Strength / Soccer / Online, one row per client with plan week, next session, check-in status and an attention badge; a row opens the Client Hub) and *Pending* (approve/deny signups, make coach). A client sees only "Connect with Your Coach" (the share panel) on the same page. |
| `client.html?uid=…` | **Client Hub** (coach only): header (name, services, client since), at-a-glance boxes (plan week, this week's completion, next session, last check-in; sessions-first for soccer-only clients), tabs *Overview* (needs attention, what's next, recent activity, their application, quick actions), *Plan* (the coach's plan workspace, `js/planWorkspace.js`: published plans with version + seen / Got it, drafts, "Take over this plan" for plans the client made, New plan, the day-by-day editor with **Details** on run days (structured run) and **Strength** on every day (`js/strengthBuilder.js`: start from a Southbound library workout, exercises with sets/reps/weight/RPE/in reserve/rest, superset with the one above, note, demo video link -- remembered per exercise in `coach-exercise-videos`, cloud-synced; a rest day with a strength session becomes a strength day; the **weekly workspace** (`js/planOps.js`, pure + unit-tested; `js/coachLibrary.js`): a ⋮ menu on every day -- Move there (swaps with a busy day), Copy there, Copy to the same day next week, Insert from library, Save to my library, Make it a rest day -- and on every week -- copy onto another week, copy last week in, save as / use a week template, clear; drag a day's label onto another day on desktop (Alt/Option copies); every change gets an **Undo** toast (latest change only). The library is the coach's saved days/weeks (`coach-workout-library`, cloud-synced, never visible to clients) plus built-in Southbound run workouts (`BUILT_IN_RUNS`, effort-based, folders Easy / Recovery / Tempo / Threshold / Intervals / Hills / Long Runs / Race Specific / Taper) and the Southbound strength sessions; a strength entry dropped on a run day is added next to the run), Save draft, Review & publish with the change list + note, version history, archive/restore), *Check-ins* (reply inline, emails the client), *Sessions* (waiting / upcoming / past with notes), *Workouts* (each logged result: planned vs actual distance/pace/effort, or for strength sets done, a planned-vs-lifted table per exercise and highlights like "top set 195 lb (planned 185)", pain highlighted, the client's note, an inline reply the client sees on the workout and in From Your Coach; badge counts unreplied recent or pain logs), *Notes* (the coach's **private notes** -- dashed sage card, lock, "Only you can see these", pin to Overview -- kept visibly apart from **updates to the client** -- tan edge, eye, "Sam sees these", read status, unsend), *Profile* (the client profile, editable by the coach). The header also shows who's training (parent accounts), "goes by", their goal + target event, and a Text button; the Overview has an "About them" card with injuries/limits highlighted, a "Pinned notes" card (private) and quick actions for "Private note" / "Send update". The Plan tab opens with the client's **change requests** (answer & resolve, emails them; answered ones fold away), check-ins show the week + wellbeing answers with an "Adjust the plan" button, and the header says "Last in the app …" (`lastSeenAt`). `?tab=plan` etc. deep-links. |
| `checkin.html` | *Review Check-ins* tab (coaches land here by default): each check-in shows the client's week + answers (`js/checkinView.js`) and an "Adjust their plan →" link. **Client side (Phase F):** a "Your week" card worked out from the week model + their workout logs (`weekSnapshot`, saved on the check-in as `week`), star rating, energy / recovery / motivation 1-5, pain + where, what went well, what to change, anything else; this week's check-in comes back to edit; history shows the coach's reply and "Your coach updated your plan after this check-in" when a newer version was published. |
| `schedule.html` | Availability, blackout dates, approve/deny booking requests |

Deep links: `?tab=pending`, `?tab=availability`, `?tab=review`, `?tab=coach`, read by each page's `selectTab()`.

`site-check.html` is a diagnostics page and is exempt from the guest redirect.

---

## How it's built

- **Static site, no build step.** Plain HTML/CSS/ES-module JS, hosted on GitHub Pages. PWA via `manifest.json` + `sw.js` (network-first for same-origin files; a page never opened online shows a branded "You're offline" page instead of a browser error). Bump `CACHE_NAME` in `sw.js` when its precache list changes.
- **Firebase** (project `eddie-s-dashboard`): Google sign-in only (`js/auth.js`), Firestore for data (`js/firebase.js`). SDK loads from `gstatic.com`.
- **Brand:** Southbound Coaching. The SB mark is traced from Eddie's own logo image (speed-streak S, open B, thin tan slash) and should keep matching it. Logo files in `brand/`: `sb-mark.svg` (cream letters, for dark backgrounds, used in every header and the page watermarks), `sb-mark-forest.svg` (dark letters, for light backgrounds), `southbound-logo.svg` / `southbound-logo-forest.svg` (full lockup with "COACHING"). App icons in `icons/` are the dark mark on cream, exactly like his image (`icon-512/192`, `icon-maskable-512`, `apple-touch-icon`, `favicon-32`). Taglines: "Faster | Stronger | Smarter" (home hero) and "Train | Develop | Compete | Grow". The old name "EddieOS" survives only in data identifiers: `eddieos-...` localStorage/cache keys, and the backup file's `app` field. Don't rename those.
- **Design system:** CSS custom properties in `css/style.css`: deep forest `--bg #0F2019`, `--surface #17291F`, tan `--primary #C9AD84`, cream `--text`/`--cream`, plus `--forest`, `--sage`, `--stone`. Text on a tan fill uses `--on-primary` (dark green), never white. Fonts: Inter (body) / Bebas Neue (display headings) / Saira 800 (`--font-brand`, the wordmark) / JetBrains Mono, **self-hosted** in `fonts/` (latin woff2, SIL OFL licenses alongside; `@font-face` at the top of `css/style.css`) so the installed app looks the same offline and nothing loads from Google. Always use `var(--font-*)`, never a font name. Category colors (run/strength/etc.) are separate on purpose. Reuse the tokens and don't hard-code new colors.
- **Controls and buttons:** `css/style.css` "FORM CONTROLS" gives every input/select/textarea/button the Southbound font, dark native pickers (`color-scheme:dark`), tan checkboxes, autofill that stays forest, one tan keyboard-focus ring, and a disabled look. Main action buttons are **tan pills** (`--radius-pill`, flat `--primary`, no gradients); small tool buttons use `--radius-sm`. New code uses `.sb-btn` + `.sb-btn-primary` / `-secondary` / `-tertiary` / `-danger` / `.sb-btn-icon`.
- **Page widths (computers):** two columns in `css/style.css` "PAGE WIDTHS": wide `--page-wide` 1200px (dashboards and tools) and narrow `--page-narrow` 900px (every `.clients-page`, Habits, Pace Calculator), both `min(..., 94%)`; the header matches the wide one. A new page gets one by using `.container` or `.clients-page`. Nothing there applies below 901px. Don't give a page body padding for its gutter.
- **Page headers:** one system for every app page, in `css/style.css` "PAGE HEADER": a small tan uppercase eyebrow (`.eos-today-eyebrow` / `.sb-eyebrow`), a Bebas title (46px, 38px on phones) and a one-line subtitle (`.eos-today-phase`). New pages use the `eos-today-hero` markup shown in that comment; older pages keep their own hero wrappers and get the same type through the grouped selectors there. Deliberate exceptions: the Client Hub's profile header and the coach's Marathon / Analytics heroes. Page watermarks use `brand/sb-mark.svg` (transparent), never the app icon (a cream square).
- **Copy:** a dash in visible text is a real one (" — "), never a typed " -- "; the product is "Southbound", never "EddieOS". A static test checks both (page text and JS strings; code comments are fine).
- **Feedback (`js/ui.js`):** `toast()` for "Saved"/"Couldn't save" (success shows only the toast; errors stay inline next to the form), `sbAlert` / `sbConfirm` / `sbPrompt` (promise-based `<dialog>`s; destructive ones pass `danger: true`, which puts focus on Cancel), `friendlyError(error, "save that")` for plain-English failures, `loadingHtml()` / `emptyHtml({...})` for designed loading and empty states. Inline page scripts use `window.SB` (loaded by `js/loadHeader.js`). **Never use the browser's `alert/confirm/prompt`** -- a static test fails on them. A placeholder that fills in after load is an empty element with class `sb-wait` (it shimmers until a script writes text), never "Loading...".
- **Reactions (custom emojis):** 24 Southbound reaction emojis in `emoji/*.svg`, drawn by `scripts/build-emoji.mjs` from one rule book (64×64, deep-forest outline, brand palette, speed streaks; the coin uses the real `brand/sb-mark.svg`). Edit the script and re-run it, never the SVGs by hand. `js/emoji.js` (pure, unit-tested in `tests/emoji.test.mjs`) is the list (stable IDs like `sb_great_work`, label, category Training / Recovery / Coach / Milestones), the one-tap `QUICK_REACTIONS`, and the text format: a message stores the token `:sb_great_work:` (no Unicode emoji, so every phone shows the same drawing and no rules change was needed), `renderEmojiText(escapedHtml)` draws known tokens (a reactions-only message shows them big), and `emojiPlainText()` turns them into "[Great Work]" (`js/emailNotify.js` applies it to every email). `js/emojiPicker.js`: any `<textarea data-emoji>` gets a React button (and `data-emoji="quick"` adds the five quick reactions) plus a live preview; the picker is a `<dialog>` (anchored on a computer, bottom sheet on a phone, stacks above other dialogs), switched on for every app page by `loadHeader`. Used on: coach workout replies, check-in feedback, updates, change-request answers (quick); the client's check-in "went well" / notes and workout log notes. Drawn wherever those texts show. Reactions are for feeling only: never in place of a working icon or of the words.
- **Icons:** `js/icons.js`. Write `<span data-icon="name">` in HTML. Content inserted later needs `import("./icons.js").then(m => m.hydrate())`.
- **Email:** `js/emailNotify.js` uses EmailJS (client-side). The free plan allows only **2 templates**, so everything runs on two: "Coach alert" (recipient fixed to Eddie's address inside EmailJS, never in this repo) for booking requests, applications and check-ins, and "Client update" (`{{to_email}}`) for booking replies, check-in feedback and coach updates (`sendCoachUpdateEmail`, links to `updates.html`). Both use `{{subject}}`, `{{headline}}`, `{{details}}`, `{{link}}` (+ `{{to_name}}` for clients). Until the IDs are filled in, emails are skipped quietly; core flows never depend on email. EmailJS's allowed-domains lock is a paid feature, so on the free plan the public key could be reused elsewhere. The mitigation: "Coach alert" only ever goes to Eddie, and "Client update" is fixed text ("your coach replied, open the app", fixed subject and link, only `{{to_name}}` varies), so it's useless for spam; "Allow EmailJS API for non-browser applications" stays off. The code still sends subject/headline/details to both templates; the client template just ignores them.
- **Fueling (`fueling.html`):** the targets math is `js/fuelTargets.js` (pure, shared with the per-workout fuel plan `js/workoutFuel.js`); targets → products (gels from the Fueling Library) → homemade drink → **Race-Day Schedule**. The schedule math is pure and unit-tested in `js/fuelSchedule.js` (`buildSchedule`, `scheduleInputFromPlan`, `diyMix`, the DIY ingredient table); `js/fuelScheduleView.js` renders it; `tests/fuelSchedule.test.mjs` covers it. Rules: gels spaced evenly from "First gel at" (default 30 min) to 15 min before the finish, caffeinated ones last; bottles split the run into equal back-to-back mile ranges with oz-per-mile sip guidance; hour-by-hour check vs targets (a leftover under 20 min folds into the last hour); warnings for gels <15 min apart, drinks >10% carbs, and bottles covering <80% of the fluid target. Saved plans open in a "View Plan" sheet (`#fuelPlanSheet`, printable) and older saved plans still render (schedule is computed from the saved fields, `firstGelMin` defaults to 30).
- **Add to Calendar (Phase H):** `js/calendarExport.js` (pure, unit-tested) turns weeks from `js/weekModel.js` into a standard `.ics` file: workouts all-day with the prescription, the one-line fuel plan and a link to `workout.html`; coach sessions timed (their start/end, else an hour); rest days and self-logged runs left out; stable UIDs so re-adding updates. `js/calendarButton.js` downloads it (the phone opens it in Apple/Google Calendar). Buttons: My Plan footer ("This week" / "Rest of the plan (N weeks)", via `sbChoose` in `js/ui.js`) and the run workout page (today or later, not yet logged). Nothing leaves the device.
- **Integrations:** COROS (`js/coros*.js`). **What COROS allows** is checked by the COROS diagnostic on Analytics: its last row lists COROS's tools (`js/corosTools.js`, unit-tested) and says whether any could send workouts to a watch; Southbound only uses read tools today. COROS replies are read by `js/corosParse.js` (unit-tested): it finds the activity list wherever it sits (activities / records / data / a bare list / one level deeper / "Key: value" text). The old reader missed nested lists and showed "no activities" to a coach who runs daily (2026-09-26). The diagnostic's "Sample activity query" now reports how many runs came back and, when none, what the reply looked like; another row lists what `querySportRecords` takes. Strava (`js/strava*.js` + the Cloudflare Worker in `cloudflare-worker/`) is **shelved**: since mid-2026 creating a Strava API app requires a paid Strava subscription, which Eddie doesn't have. The code stays; its Analytics panels hide themselves until `js/stravaConfig.js` is filled in.

### Firestore data model

| Collection | What it holds |
|---|---|
| `users/{uid}/sync/localStorage` | Each user's private app data, mirrored from localStorage (`js/cloudSync.js`, last-write-wins per key) |
| `userProfiles/{uid}` | `role`, `isCoachApproved`, `status` (`pending`/`active`/`archived`), `services[]`, application fields, `lastSeenAt` (Phase F: the client's app stamps it at most every 6 hours via `touchLastSeen()` from `js/loadHeader.js`; the rules accept only the server's clock). Readable only by the owner and approved coaches. Users may only edit their own application fields; role/status/services/isCoachApproved are coach-only. |
| `inviteCodes/{code}` | One-time codes a client gives a coach. Never listable; expire after 7 days. |
| `coachLinks/{coachUid}_{clientUid}` | Proof a client linked a coach. It gates everything below. Only an approved coach can create one, in the same batch that deletes (burns) a valid code from that client (`redeemInviteCode` in `js/coachAccess.js`). |
| `sharedPlans/{clientUid}` | Mirror of the client's training/race plans + coach notes |
| `coachAvailability/{coachUid}` | Weekly slots + blackout dates |
| `bookingRequests/{id}` | Session requests (single or recurring). Only the coach can approve/deny; the client can only cancel; booking details never change after creation. |
| `checkins/{clientUid}_{weekOf}` | One weekly check-in per client: rating + notes, and (Phase F) energy/recovery/motivation 1-5 or null, pain + painNote, wentWell, change, `week` (the weekSnapshot map, <=16 keys); then coach feedback. Create is limited to known keys (`validCheckin` in the rules); older app versions sending only rating + notes still pass. |
| `changeRequests/{id}` | A client asking to change the plan (Phase F, `js/changeRequests.js`): `{ clientUid, clientName, coachUid, planId|null, date|null, reason: work/fatigue/travel/pain/event/other, message (1-1000), status open/resolved, coachReply, createdAt, resolvedAt }`. The linked client creates (status open, no reply), may delete it while open; the coach may only set `coachReply`/`status`/`resolvedAt` (server time when resolving). New ones email the coach; answers email the client. |
| `clientRecords/{clientUid}` | The **client profile** (fields in `js/clientRecordSchema.js`): who's training (self/child), preferred/athlete name, birth year, phone, sport, team/level, current training, weekly miles, strength experience, available days + notes, main/other goals, target event + date, what they want from a coach, what has/hasn't worked, injuries. Plus `intakeComplete`, `intakeCompletedAt`, `updatedAt`, `updatedBy`. The client and their **linked** coach can read and write it (`validClientRecord` checks every key/type/size and that updatedBy/updatedAt are honest); nobody deletes it. **Nothing coach-private goes here** -- the client can read it all. Name/email/services/status/start date stay on `userProfiles`/`coachLinks`, not duplicated. |
| `coachNotes/{id}` | The coach's **private** notes about a client: `{ coachUid, clientUid, text (<=4000), pinned, createdAt, updatedAt }`. Readable **only by the coach who wrote it** -- the rule has no client clause at all, and the client's own query is denied. Create needs a coachLink; update may only change text/pinned/updatedAt (can't re-point to another client). |
| `clientUpdates/{id}` | Updates the coach sends a client: `{ coachUid, coachName, clientUid, text (<=2000), createdAt, readAt }`. The linked coach creates (readAt must start null) and deletes ("Unsend"); the client reads and may only set `readAt` to the server time. `js/clientNotes.js` handles both collections. |
| `coachingPlans/{planId}` | **Coach-owned published plans** (coaching Phase A). Header: coachUid, coachName, clientUid, name, kind (race/training/custom), status (active/archived), version, publishedAt, coachNote, changes[] (the readable change list), start/endDate, adoptedFrom ({store,id} of the client's own plan it replaced, or null), viewedVersion/At + ackVersion/At (written only by the client, only forward, server-stamped). Created at the first publish, in the same batch as version 1; each publish bumps `version` by exactly 1 alongside its version doc (rules check both). Never deleted -- archived. |
| `coachingPlans/{planId}/versions/{n}` | Each published version: the prescription only (`plan`, same shape as a generatedPlan, no completion/results), changes, coachNote. Written once, never updated or deleted. The client and the coach can read them. |
| `coachingPlanDrafts/{planId}` | The coach's unpublished work (same planId as the plan it will become). **Coach only** -- no client clause; the client can't even query them. Deleted on publish. |
| `workoutResults/{clientUid}_{planId}_{date}[_strength]` | What the client did on a day of a coach plan (coaching Phase C, `js/workoutResults.js`; a strength log, Phase D, has the `_strength` suffix, `kind: "strength"` and `exercises: [{ name, sets: [{ weight, reps }] }]` (<=20, rules-checked), so a run and a lift on the same day don't collide): `{ clientUid, coachUid, planId, planVersion, date, title, plannedMiles, status: completed/skipped, distance, durationSec, rpe 1-10, pain, painNote, note, createdAt, updatedAt, coachComment, coachCommentAt }`. The client creates it (the doc ID, the plan's client/coach and a coachLink are all checked), edits its own fields and may delete it; the linked coach may only set `coachComment`/`coachCommentAt`. Kept apart from the prescription on purpose. The device copy of the plan day also gets `completed`/`skipped`/`resultId`/actuals so Today and My Plan agree. A new pain flag emails the coach. |
| `inquiries/{id}` | Questions from `contact.html`. **Anyone can create one without signing in**, so the rules validate every field (known keys, sizes, email format, an email or a phone, `status: "new"`, server timestamp). Only approved coaches can read them; the coach can only flip `status` new/handled. A hidden honeypot field in the form drops obvious bots. If spam ever becomes a problem, add Firebase App Check. |

Services vocabulary (`js/userProfile.js`): `online_coaching`, `running`, `strength`, `soccer_1on1`, `soccer_group`.

### Eddie's personal plan vs. clients

`js/marathonData.js` is Eddie's own race block (Indianapolis, 3:05). It must only show for the coach. `js/role.js` remembers the account's role per device (`sb-account-role` in localStorage, written by `js/navAccess.js`, not cloud-synced). Pages that render before the profile loads call `showsPersonalPlan()`; `js/loadHeader.js` reloads the page once (guarded by sessionStorage `sb-role-reloaded`) when the role it just learned differs from what the page rendered with. Unknown role = client. For clients: Today, Running, Strength, Cross-Training, Fueling and Nutrition use the client's own Programs (`getActiveProgramEntriesForDate`) and logged miles instead of the marathon plan, and the marathon picker/overlap checks are off.

### Client Today + coach link

- **"From your coach" card** (`js/coachCard.js`, `#coachCardSection` in `index.html`, clients only): next approved session, requests waiting, notes from the last session (`coachNote`), weekly check-in due/sent, latest feedback. Reads only data the client can already read.
- **Auto-link on approval, no rules change:** an applicant's app leaves a standing invite code `APPLY-{uid}` (`ensureApplyCode()` in `js/coachAccess.js`, refreshed by `loadHeader` while pending and by `apply.js` after submitting; recreated when older than 5 days). The coach's Approve (`js/clients.js`) then calls `linkApplicant(uid)`, which redeems it through the normal code path (`linkWithCode`). If it fails, the alert falls back to the old "ask for an invite code" instructions.
- **Session notes:** the coach can add/edit notes on approved bookings (`setSessionNotes` in `js/scheduling.js`, allowed by the existing rules); the client sees them on Schedule and the Today card.
- **Group capacity:** the coach's request rows show "x/y already booked -- full", and approving into a full slot asks first. Clients can't see spots left (that would need a rules change).
- **One view per role, no tab bars:** Schedule (coach: availability + requests; client: Book a Session), Check-in (coach: review queue; client: their check-in) and My Clients (coach: clients + pending; client: connect code) each pick their panel from `js/role.js` first, then confirm from the profile.
- **Linking without applying:** clients who didn't come through `apply.html` use More → Connect with Coach to get a code; the coach enters it under Coach a Client.
- **Habits:** the coach keeps his own starting list (ids h1-h10, which his saved check-offs use); clients start with a generic one (c1-c5) in `js/habits.js`.
- **Today stats for clients:** "Recovery" (COROS) becomes "Sessions Booked" (upcoming approved sessions, returned by `renderCoachCard`).

### Client Hub (coach CRM) -- see `docs/CLIENT_MANAGEMENT_PLAN.md`

Eddie's handoff for turning Southbound into a connected client-management platform ("powerful underneath, simple on the surface"), in 9 phases. **Phases 1-3 are built.** Phase 1 (Client Hub foundation) stores nothing new -- it reads what a linked coach can already read. Phase 2 adds the client profile (`clientRecords`, `js/clientRecordSchema.js` + `js/clientRecords.js` + the shared form `js/clientProfileForm.js`, used by `profile.html` and the hub's Profile tab). Birth *year*, not birthday: it's all coaching needs (age groups, zones) and less sensitive. A test (`tests/clientRecordSchema.test.mjs`) fails if the schema's fields and the rules' allowed keys drift apart.
- `js/clientDirectory.js` loads coachLinks + userProfiles + sharedPlans + checkins + bookingRequests (existing modules).
- `js/clientSummary.js` (pure, unit-tested in `tests/clientSummary.test.mjs`) derives: plan position (current week, start/end), today/next workout and this week's completion from the mirrored plan days' `completed` flags, sessions (upcoming/past/waiting), check-ins (latest, needs reply, recent average), needs-attention items, and a recent-activity timeline built from existing timestamps.
- `js/planEditor.js` is the plan editor, moved out of the old My Clients popup; the hub's Plan tab mounts it inline.
- Phase 3: private notes (`coachNotes`) and client updates (`clientUpdates`) are **two separate collections on purpose**, so a private note can never reach a client by a missed filter or a UI slip. `buildCoachFeed` (`js/clientSummary.js`, unit-tested) merges updates + check-in replies + past session notes for `updates.html`; the Today coach card shows "New update from ..." until it's read; the hub timeline shows sent/read.
- Next phases: 4 stored timeline, 5 "Needs attention" coach dashboard, 6 session history/mark complete, 7 progress, 8 client side, 9 packages/billing. Phases 2+ add collections, so they need rules + rules tests + Eddie pasting the rules.
- **Progress data limit:** a client's own logs (running-log, habits, nutrition) live in `users/{uid}/sync`, which only the client can read. The hub can only show what the client's app mirrors to `sharedPlans` (plans + completed days). Showing more (Phase 7) means mirroring a small summary, not opening the private sync doc.

### Navigation access

`js/navAccess.js` turns the profile into `{ isCoach, hasTrainingAccess, hasSoccerAccess, status }`. HTML elements carry `data-requires="..."` with any of `training`, `soccer`, `coach`, `client` (meaning a non-coach with training or soccer access). Multiple values are OR'd. Mobile bottom tabs are defined in `BOTTOM_TABS` in `js/loadHeader.js`: Today, Plan (clients with training access, `requires: "client-training"`), Train, Health, Habits, Coach (coach only), More. **Nav access fails open:** if the profile can't load, the full nav shows rather than an empty one.

---

## Gotchas we've already hit (don't repeat them)

- **`[hidden]` vs CSS `display`:** any class that sets `display` beats the browser's native `[hidden]`. Every page/component scope has a blanket override (`.clients-page [hidden]{display:none!important}` etc.). Add one when writing new CSS.
- **Flex rows with wrapping text:** use `align-items:flex-start`, or action buttons float to the middle of tall rows.
- **Flex wrapping:** `flex:1` (basis 0) overrides `width:100%`. Use `flex:0 0 100%` to force an item onto its own row.
- **Reload loop:** comparing Firestore data with plain `JSON.stringify` caused an infinite reload (key order changes). Use `stableStringify` in `js/coachAccess.js`.
- **Guest redirect** only fires when Firebase definitively says "signed out". It never fires on an auth-load error, so offline users aren't kicked out.
- **Overlays** need `z-index` above the navbar (1000) and search overlay (4500). The plan editor uses 5000.
- **Cards with `overflow:hidden` inside a scrolling flex column shrink to slivers** (their flex min-height becomes 0). Give them `flex-shrink:0` -- this was the live-workout "things overlap" bug.
- **The domain move changed the origin.** Browser storage (localStorage, the installed app, COROS tokens) is per-origin, so it doesn't carry over from the github.io address; signed-in users get their data back from cloud sync on the new domain. Google sign-in only works on domains listed in Firebase Console → Authentication → Settings → Authorized domains.
- **Honeypot fields must not look like real fields.** The contact form's hidden spam trap was once labeled "Company"; browser autofill filled it, so real questions were silently dropped while showing "Question sent". Keep it as `sbLeaveEmpty` ("Leave this empty", `autocomplete="off"` + password-manager ignore attributes).
- **Hand-uploaded files can drop rules.** A 2026-09-17 upload replaced `css/nutrition.css` and silently removed the barcode-scanner modal, food log and confirm styles (restored 2026-09-25). If Eddie uploads a file, diff it against the previous version.
- **Phone inputs must be 16px+** or iOS zooms on focus. `css/style.css` forces 16px on form fields at <=900px (the big fueling-target and record inputs are excluded); small buttons get ~40px touch targets there too.
- **Reading your own not-yet-created doc is permission-denied, not "missing".** Rules like `checkins` check `resource.data.clientUid`, which fails when there's no doc. `submitCheckin` read first, so every first weekly check-in failed on the live site until 2026-09-25. Treat permission-denied on your own doc ID as "new", and test the real create path, not an admin-seeded doc.
- **Page CSS resets body padding on phones.** Several pages set `body{padding:20px}`, which wiped out the bottom tab bar's padding. `loadHeader` now adds `body.has-bottomnav`, padded with `!important` in `css/style.css`.
- **Phone overrides in `css/style.css` need `!important`.** Page stylesheets load after it, so equal-specificity rules there win (yesterday's first tap-target pass silently didn't apply).
- **Any rules change needs a test.** Add the attack and the legitimate flow to `tests/rules.test.mjs`.
- **A coached plan's source of truth is `coachingPlans`, not the client's device.** Never write a coach plan into `running-programs`/`training-programs` or edit it through `sharedPlans`; publish a new version. The device copy (`coach-plans`) is a cache plus the client's own records; completion/results never go into a published version (`stripRuntime`).
- **Firestore can't query a subcollection without a filter the rules can check.** Listing `versions` needs `where("coachUid"|"clientUid", "==", uid)` (`listVersions`).
- **`sbConfirm`/`sbPrompt` are async.** Swapping one in for `confirm()` means the enclosing function/listener must be `async` (and callers mustn't rely on a sync return). `node --check` catches a stray `await`.
- **`.sb-wait` placeholders shimmer until they have text.** If code can leave one empty on purpose, give it text or remove the class, or it shimmers forever.
- **The `.primary-btn`/`.coach-button` tan-to-blue gradient** (`#2D7FFF`, pre-rebrand) was still live until 2026-09-25. Search for gradients before assuming the palette is clean.

## Testing

- `npm install` once, then `npm test` runs everything: `npm run test:static` (every script parses, every local link/asset/import exists, public pages have meta descriptions and no placeholder text, `site-manifest.json` is current) and `npm run test:rules` (the Firestore emulator against `firestore.rules`). GitHub Actions (`.github/workflows/ci.yml`) runs both on every push.
- After adding or removing a page, script or stylesheet, run `npm run manifest` (it feeds `site-check.html` and the static tests).
- The sandbox can't reach `gstatic.com`, so the real Firebase SDK won't load. Test with Playwright (installed globally at `/opt/node22/lib/node_modules/playwright`, browsers at `/opt/pw-browsers`) by serving the repo with `python3 -m http.server 8934` and mocking `js/auth.js`, `js/firebase.js`, `js/userProfile.js`, `js/cloudSync.js`, etc. via `page.route()`.
- **Create contexts with `serviceWorkers: "block"`,** or the service worker serves the real modules and bypasses your mocks.
- **Start the emulator from the repo folder** (`npx firebase emulators:exec ...` with the repo as the working directory). From anywhere else it finds no `firebase.json`, runs with **no rules at all**, and every privacy check ("client can't read coach notes") falsely fails.
- Screenshot at desktop (1280px) **and** phone (375–390px) widths, and look at them.
- Mock data must match the real shape (e.g. list functions return `{ id, ...data }`).
- `node --check file.js` for quick syntax checks.

---

## Current status (as of 2026-09-25)

Roadmap steps 1–7 are done and live on `main`: audit, account/profile model, coach approval, public site, service-driven nav, coach dashboard, weekly check-ins. After that came the unified Coach nav section and the guest-view redesign (photo-led Home, About, Packages, Get the App in the top nav). Then a security review's fixes (rules + atomic invite-code redemption), the live-workout layout fixes on phones, automated tests + CI, and launch polish (meta descriptions, interim bio/pricing copy, setup checklist on the Coach Dashboard). Then the Southbound Coaching rebrand (SB mark traced from Eddie's logo, forest/tan palette, new app icons) and the two-template EmailJS setup.

**2026-09-25 batch (no rules change):** personal plan hidden from clients, client Today "From your coach" card, auto-link on approval, session notes, group capacity checks, client Schedule view, phone polish (no input zoom, bigger tap targets, nutrition +/- row), restored nutrition scanner/food-log CSS, privacy page. Regression harness: an emulator-backed Playwright run (real modules + rules) covering pending → approve → linked → client Today/Running/Fueling/More/Nutrition/Schedule, coach Today/Schedule, guest privacy + redirect.

**2026-09-25 audit batch (no rules change):** full audit of every page as guest/client/coach at 1280 and 390 against the emulator, then fixes: first weekly check-in always failed (fixed + rules test), clients' default habits were Eddie's personal list, unlinked clients had no way to get a link code (now More → Connect with Coach), bottom tab bar covered the end of 5 pages, clients could open Eddie's pages by URL, Fueling/Programs/Settings copy, Settings controls that did nothing removed (units, week start, goal time, weekly mileage, AI Coach), COROS shown under Settings for the coach only, "Synced never synced", role-specific single views, tap targets/text sizes (stars 44px, 40px buttons, no text under 10px), Fueling library/pre-workout fold on phones, coach stat grid 2-up, labelled slot fields.

**Client Hub Phase 1 (2026-09-25, no rules change):** searchable/filterable client list, `client.html` hub with Overview / Plan / Check-ins / Sessions, plan editor moved inline, 9 unit tests + an emulator e2e (list, search, filters, hub summary, plan edit saves to sharedPlans, inline check-in reply, soccer-only view, unknown uid, client redirected).

**Client Hub Phase 2 (2026-09-25, RULES CHANGED):** client profile (`clientRecords`) with `profile.html` intake (prefilled from the application, parent/child aware), Today prompt, hub Profile tab + header goal/phone + About card, profile-aware attention/timeline/search, privacy page updated. 3 new rules tests, 9 new unit tests, a 23-step emulator e2e.

**Client Hub Phase 3 (2026-09-25, RULES CHANGED):** private coach notes (pin to Overview, edit, delete) and client updates (send + email, read status, unsend) on a new hub Notes tab; client "From Your Coach" page (`updates.html`) + Today row + More row; privacy page mentions coach notes. 3 new rules tests, 1 new unit test, a 35-step emulator e2e.

**Polish foundation (2026-09-25, no rules change):** first batch of `docs/APP_POLISH_PLAN.md`. Self-hosted fonts, normalized form controls and focus states, one button system (fixed a leftover tan-to-blue gradient), `js/ui.js` dialogs/toasts replacing all 74 browser alert/confirm/prompt calls, shimmer loading states instead of "Loading...", designed empty state, branded offline page, two new static guards (no browser dialogs; fonts self-hosted). A 35-step polish e2e plus every earlier suite pass. The full page-by-page polish audit comes after the coaching platform (see What's next).

**Coaching Phase A (2026-09-25, RULES CHANGED):** the coach owns the prescription. Draft -> Review & publish -> immutable version; the client's app copies the newest version to `coach-plans` (`js/coachPlanSync.js` from `js/loadHeader.js`, one reload when something changed) and keeps its own done marks by date (`mergeRuntimeByDate`); taking over a client-made plan retires their copy (`adoptedFrom`). The calendar/Today/Running read coach plans through `js/activeProgramSources.js` (they're never listed on Programs/Race Plans, so a client can't edit or delete one). My Plan page + Got it; hub attention "hasn't opened your update" after 2 days; timeline published / got it. The old coach editor that wrote into the client's `sharedPlans` copy (`js/planEditor.js`, `saveClientPlanList`) is gone. 5 rules tests, 9 model + 5 summary unit tests, a 50-step emulator e2e (take over, draft, publish, client sync with marks carried, Got it, move a long run -> v2 "moved from Sat to Sun", new blank plan, archive).

**Coaching Phase B (2026-09-25, no rules change):** one week for the client. `js/weekModel.js` (pure, unit-tested) merges plan days + a plan's extra sessions + the Strength schedule (a plan's strength session copied onto the schedule -- tagged racePlanId/trainingPlanId, or the plan a coach plan took over via `adoptedFromId` -- shows once) + approved sessions + logged runs (a planned run counts as done when they logged at least 80% of it that day, `autoDone`, not saved as a mark); states done / today / missed / upcoming / rest; week totals; next workout. `js/weekData.js` gathers the inputs and saves Mark done to the right store; `js/weekView.js` renders it for Today and My Plan. Client Plan tab. 5 unit tests + a 23-step emulator e2e.

**Coaching Phase C (2026-09-25, RULES CHANGED):** structured running workouts + results. `js/runWorkout.js` (pure, unit-tested: paces, set text, planned miles, execution steps, planned-vs-actual) defines `day.workout` = warm-up, sets (reps x distance/time, pace range or effort, recovery), cool-down, why, cue. The coach builds it with **Details** on a run day in the plan editor (`js/workoutBuilder.js`); publishing sets the day's session text and miles from it. The client opens `workout.html` from the week: steps, why/cue, a full-screen workout mode (step timers, keeps the screen on) and a log form; results go to `workoutResults` (rules above), a skipped day shows "Skipped" (not missed), a pain flag emails the coach and tops the hub's Needs attention until replied. Hub Workouts tab + timeline entries; coach replies show on the workout page and in From Your Coach. 2 rules tests, 8 unit tests, a 53-step emulator e2e (build a structured workout, publish, client runs it in workout mode, logs pain, coach sees it first and replies, client sees the reply, skip shows as Skipped).

**Coaching Phase D (2026-09-25, RULES CHANGED):** coach strength sessions. `js/strengthWorkout.js` (pure, unit-tested: cleaning, 1/2/3A/3B labels, set-by-set order with supersets alternating, planned vs actual, safe https-only video links) defines `day.strength` = title, minutes, goal, notes, exercises. It sits on any plan day: the day itself on a strength day, or an extra session on a run day, with its own runtime marks (`strengthCompleted/Skipped/ResultId/Rpe/Pain` in `RUNTIME_KEYS`). `dayText` includes the full prescription, so a weight change shows in the publish change list. The week model shows it as its own item (`source.type` "plan-strength" for extras; Mark done toggles `strengthCompleted` in `coach-plans`). Strength logs are `workoutResults` with `_strength`; the hub's plan summary only lets a strength log decide a strength-only day. Not built yet: per-set prescriptions (pyramids), percentage-of-max loads, saving the coach's own sessions as reusable templates, and a strength history/progression view (the logs are stored so it can come later). 1 rules test, 7 unit tests, a 45-step emulator e2e (template -> edit -> superset -> rest day becomes strength day -> publish -> Today cards -> session page -> guided session with rest timer, weight carried forward, a skipped set -> log -> planned vs actual -> skipped core -> My Plan -> coach table + highlights + reply -> client edits the log).

**Coaching Phase E (2026-09-26, no rules change):** the coach weekly workspace. `js/planOps.js` (pure, unit-tested): a day's prescription = type/miles/session/workout/strength; move = swap, copy day / to next week, clear, copy/clear week, library entries for a day or a week, apply them, 13 built-in Southbound run workouts. Dates and the client's marks never move. The day/week ⋮ menus, library picker (search, folders, delete), desktop drag and drop, and Undo (toast action, added to `toast()` in `js/ui.js`) live in `js/planWorkspace.js`; publish, change list and Got it are Phase A's. 5 unit tests + a 33-step emulator e2e (move, undo, drag, copy, save/insert/search/delete library, built-ins, strength onto a run day, week template save/apply, clear + undo, copy week, publish shows "moved from", phone menu, client sees the moved structured run).

**Coaching Phase F (2026-09-26, RULES CHANGED):** the feedback loop. `js/feedbackModel.js` (pure, unit-tested): `weekSnapshot` (a logged result decides its coach-plan day even without this device's mark), `snapshotLines`, `cleanCheckinAnswers`, `checkinFlags` ("Pain: …", "Low recovery (2/5)", "Wants a change"), `CHANGE_REASONS`, `attentionQueue` (`ATTENTION_ORDER`: pain, change, checkin, missed, skipped, booking, plan-unseen, race, plan, quiet, no-checkin, sessions, intake). `needsAttention` gained change requests, check-in flags in the check-in item, race within 14 days, and "Hasn't opened the app in N days" (7+). Client: smarter check-in, **Need a change?** on My Plan and "Can't do this one?" on workout pages (`js/changeRequestDialog.js`), requests + answers on My Plan and in From Your Coach. Coach: dashboard queue, hub change requests, richer check-ins, last seen. Also made the Phase B / hub harnesses weekend-proof (pinned clocks). 3 rules tests, 5 unit tests, a 42-step emulator e2e.

**Coaching Phase G (2026-09-26, no rules change):** fuel on the workout. `js/fuelTargets.js` is the Fueling page's targets math, moved out of `js/fueling.js` unchanged. `js/workoutFuel.js` (pure, unit-tested): `fuelForRun({ type, miles, durationMin?, coachNote }, { profile, library })` -> level none / light (60-75 min) / full (75+ min, or any long run / race), duration from miles x the type's pace, targets, gels via `buildSchedule` (their Fueling Library gels, a generic 25 g gel if none, caffeine only on full plans and last), before (scaled by body weight when known) / during / after, a one-line summary; `profileFromPlans` takes the athlete's inputs from their newest saved fueling plan. Shown on the workout page, as "Fuel: 3 gels, first at 0:30 · 21 oz/hr" on Today's run card (`fuelForItem` in `js/weekData.js`), and as gel cues in workout mode. The coach's **Fueling note** is a field in the run Details builder (`workout.fuel`). 4 unit tests + a 19-step emulator e2e.

**Polish audit (2026-09-26, no rules change):** every page swept as guest / client / coach at 390 and 1280 (overflow, text under 11px, unnamed buttons, dev words, stuck placeholders, screenshots). Fixed: one page-header system (eyebrow + Bebas title + subtitle) across 22 app pages, eyebrows added where missing; "EddieOS" label on Analytics; " -- " in 52 visible strings (public pages, toasts, fuel plans) replaced by real dashes, with a static guard; text under 11px (bottom tab labels, calendar marks and day names, badges, tags); the search button's accessible name; empty Strength calendar filler cells were focusable buttons; the Pace Calculator's tabs ran off phones (now share the row), My Clients filter chips wrap, the coach section row scrolls the current page into view; the client Running page showed "Rest" on coach-plan days and listed upcoming runs twice and out of order; app-icon watermarks showed as a light square (now the transparent mark); Weekly Review's "Loading…" placeholder. Left on purpose: side-scrolling week rows (Fueling week tabs, Marathon week chips and chart), the logo's small "Coaching" line. Not done yet: page content widths still differ from page to page (header vs. content column).

**Custom reactions (2026-09-26, no rules change):** the last part of `docs/APP_POLISH_PLAN.md`. 24 hand-drawn SVG reaction emojis (vector instead of the plan's image-generation route: crisp at every size, one exact palette, tiny files, offline), the `:sb_id:` token format, the Southbound picker with quick reactions and a live preview, drawn on the workout page, strength session, From Your Coach, the Today coach card, My Plan change answers, check-in history and review, and the hub; emails show "[Label]". 4 unit tests + a 23-step emulator e2e (quick reaction, picker open/Escape/choose, saved tokens, big reaction-only update, email labels, client views, phone bottom sheet above the log dialog, check-in reaction seen by the coach).

**Client fixes (2026-09-26, no rules change):** Nutrition goals: "Edit Goal" was never saved (a reload reset it) and every client started from Eddie's own numbers. Now `js/nutritionGoals.js` (pure, unit-tested) gives the coach his numbers and a client a starting point (scaled to the body weight on their newest Fueling plan: ~1.6 g/kg protein, 5 g/kg carbs, 0.8 g/kg fat, half their weight in oz of water; otherwise 2200 kcal / 110 g / 275 g / 70 g / 80 oz / 2300 mg) with a "Starting goals" note until they set their own; goals save as `nutrition-goals` (cloud-synced). Page widths unified (above). "75-Day Operating System" renamed "75-Day Challenge" (static guard added), its loading text shimmers instead. 4 unit tests + a 10-step emulator e2e.

**Coaching Phase H, part 1 (2026-09-26, no rules change):** Add to Calendar (above) on My Plan and the workout page; session items now carry `startTime`/`endTime`; `sbChoose` dialog; the COROS capability check. 7 unit tests + a 13-step emulator e2e (dialog choices, this-week file contents, all-day workouts with links and fuel, timed session, rest of the plan, cancel, single workout, none on a logged past run, COROS check against a stand-in server).

**Known gaps / Eddie's call (not done):**
- COROS for clients and sending workouts to watches: waiting on Eddie running the COROS diagnostic on Analytics ("What COROS lets Southbound do") with his account. If COROS only offers read tools, watch sync means applying for COROS / Garmin partner API access (approval + a server, so the Blaze plan). Today COROS works only for the coach (`analytics.html`; Settings shows it to the coach only).
- Nutrition goals are the client's own (starting point or what they set). The coach can't set them for a client yet; that would need new fields and a rules change.
- The old school/course dates were removed from the code on 2026-09-25, but they're still in the public repo's git history. Scrubbing that means rewriting `main`'s history (force-push); only with Eddie's go-ahead.

**Waiting on Eddie:**
- [x] Eddie's own account is an approved coach (the Coach Dashboard works for him).
- [x] Firestore rules published (2026-09-24, including `inquiries`). Re-paste the whole file whenever it changes.
- [ ] **Publish the updated rules (Phase F: `changeRequests`, richer `checkins`, `lastSeenAt` on `userProfiles`; strength logs in `workoutResults` for coaching Phase D, `workoutResults` for coaching Phase C and `coachingPlans` + `coachingPlanDrafts` for coaching Phase A; also includes `clientRecords`, `coachNotes`, `clientUpdates`).** Until then profiles, notes, updates, coach plans and workout logs can't be read or saved; the app degrades quietly (no intake nag, profile save says "isn't switched on yet", the Notes tab says the rules may not be published).
- [ ] Photos: the hero, all four offer cards and gallery-1 are Unsplash stock (free license, graded to match; hero alt text doesn't claim it's Eddie). Still needed from Eddie: `eddie-portrait.jpg`, `eddie-about.jpg`, `gallery-2/3.jpg` (filenames in `images/README.md`). Swap the hero for a real photo of Eddie when he has one.
- [ ] Send real **prices** for `packages.html` (monthly coaching, per soccer session, 5-pack, 10-pack, group drop-in, group monthly). Until then each package says "Pricing on request".
- [ ] Send a short **bio** for `about.html` (background, experience, certifications). It currently has honest interim copy with no specific claims.
- [x] EmailJS is connected (service + "Coach Alert" / "Client Update" templates + public key in `js/emailNotify.js`). Emails come from Eddie's Gmail as "Southbound Coaching".

## What's next (roadmap)

**Active track: Client management** (`docs/CLIENT_MANAGEMENT_PLAN.md`) -- Phases 1-3 done; Phase 4 (stored timeline) next.

**Queued plans from Eddie (2026-09-25). Agreed order: polish foundation (done), coaching platform phases A-G (done), the full polish audit and the custom reactions (both done 2026-09-26); the email switch whenever his accounts are ready:**
- `docs/COACHING_PLATFORM_PLAN.md`: the coach-delivery platform. The coach owns a published plan in Firestore (draft/publish, versions, acknowledgement); the client gets a unified Today / My Week / workout detail; structured run and strength workouts with planned-vs-actual results. Its phases A-I absorb Client Management phases 4-9.
- `docs/APP_POLISH_PLAN.md`: official-app polish (one design system, native controls, states, toasts, copy) plus a custom Southbound reaction-emoji set. **Done:** the foundation batch, the page-by-page audit and the custom reaction set. Page widths were unified afterwards.
- `docs/EMAIL_NOTIFICATIONS_PLAN.md`: replace EmailJS with Firebase Cloud Functions + Resend, driven by Firestore notification events. Needs the Firebase Blaze plan, a Resend account and DNS records for the domain, all set up by Eddie.

8. **Training ↔ fueling connection.** Done (coaching Phase G): every planned run has its Before / During / After fuel plan. Possible next steps: daily nutrition targets on the Nutrition page that follow the training load, and bottles / drink mix in the per-workout plan.
9. **Booking refinements:** location, session length. (Done: the public question path `contact.html`, session notes, group capacity checks.)
10. **Business tools:** payments, packages checkout.

The brand is Southbound Coaching (decided 2026-09-24 from Eddie's brand board: SB speed monogram, forest green / sage / tan / stone / cream).
