{
  "name": "eddie-dashboard",
  "private": true,
  "description": "Dev-only tooling for tests. The site itself is static and needs no build step.",
  "type": "module",
  "scripts": {
    "test": "npm run test:static && npm run test:rules",
    "test:static": "node --test tests/static.test.mjs tests/fuelSchedule.test.mjs tests/clientSummary.test.mjs tests/clientRecordSchema.test.mjs tests/coachingPlanModel.test.mjs tests/weekModel.test.mjs tests/runWorkout.test.mjs tests/strengthWorkout.test.mjs tests/planOps.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-eddie \"node --test tests/rules.test.mjs\"",
    "manifest": "node scripts/build-manifest.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.0.0"
  }
}
